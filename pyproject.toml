[project]
name = "standalone-image-worker"
version = "1.0.0"
description = "Standalone local JSON-RPC STDIN process for high-speed image processing."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "Pillow>=11.0.0",
  "pytesseract>=0.3.13",
]
